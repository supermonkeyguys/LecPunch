export const SKIN_PRICE_POINTS = 600;

// Client-generated imported skin ids and bundled skin ids must stay in this
// small, URL-safe namespace. The server does not receive paths or filenames.
export const SKIN_ID_REGEX = /^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$/;
