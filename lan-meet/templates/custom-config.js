// Appended by the official Jitsi web image during startup.
// Keep this file free of credentials and public Internet endpoints.
config.p2p = { enabled: false };
config.analytics = { disabled: true };
config.disableThirdPartyRequests = true;
